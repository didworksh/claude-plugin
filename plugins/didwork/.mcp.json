{
  "mcpServers": {
    "didwork": {
      "command": "npx",
      "args": [
        "-y",
        "@didwork/mcp@0.0.8"
      ],
      "env": {
        "DIDWORK_API_KEY": "${DIDWORK_API_KEY}"
      }
    }
  }
}
